{
  "name": "backend",
  "version": "1.0.0",
  "description": "backend for croissant",
  "main": "app.js",
  "dependencies": {
    "axios": "^0.21.1",
    "cookie": "^0.4.1",
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "express-session": "^1.17.1",
    "mongodb": "^3.6.4",
    "socket.io": "^4.0.0",
    "validator": "^13.5.2"
  },
  "devDependencies": {},
  "scripts": {
    "start": "node app.js",
    "heroku-postbuild": "cd client && npm install --only=dev && npm install && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "engines": {
    "node": "12.19.0"
  },
  "author": "",
  "license": "ISC"
}
